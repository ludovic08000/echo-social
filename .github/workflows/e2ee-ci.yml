name: E2EE CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint linked-device code (diagnostic)
        continue-on-error: true
        run: >-
          npx eslint src/hooks/useDeviceLink.ts
          --format json --output-file eslint-device-link.json

      - name: Upload lint report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: eslint-device-link
          path: eslint-device-link.json
          retention-days: 1

      - name: Test hardened ratchet
        run: npm run test -- src/lib/crypto/__tests__/signalProtocolHardening.test.ts

      - name: Build
        run: npm run build
