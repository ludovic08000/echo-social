name: Aegis final RPC correction

on:
  push:
    branches:
      - agent/aegis-ui-rpc-logic-fix

permissions:
  contents: write

jobs:
  patch-test-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/aegis-ui-rpc-logic-fix
          fetch-depth: 0

      - name: Apply final RPC alignment
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          fanout_path = Path('src/lib/messaging/multiDeviceFanout.ts')
          fanout = fanout_path.read_text()
          old_lookup = """  const rpcResult = await supabase.rpc('get_device_copy_for_message', {
              p_message_id: messageId,
              p_device_id: deviceId,
            });"""
          new_lookup = """  const rpcResult = await supabase.rpc('get_device_copies_for_messages', {
              p_message_ids: [messageId],
              p_device_id: deviceId,
            });"""
          if fanout.count(old_lookup) != 1:
              raise SystemExit('Expected exactly one singular device-copy lookup')
          fanout_path.write_text(fanout.replace(old_lookup, new_lookup))

          transport_path = Path('src/lib/messaging/aegisTransport.ts')
          transport = transport_path.read_text()
          old_names = """type AegisRpcName =
            | 'aegis_ack_device_messages'
            | 'aegis_send_message'
            | 'aegis_sync_device';"""
          new_names = """type AegisRpcName = 'aegis_send_message';"""
          if transport.count(old_names) != 1:
              raise SystemExit('Expected exactly one obsolete Aegis RPC union')
          transport_path.write_text(transport.replace(old_names, new_names))

          test_path = Path('src/lib/messaging/__tests__/aegisUiRpcCompatibility.test.ts')
          test_path.write_text("""import { readFileSync, readdirSync } from 'node:fs';
          import { extname, join, normalize } from 'node:path';
          import { describe, expect, it } from 'vitest';

          const read = (path: string) => readFileSync(path, 'utf8');
          const generatedSupabaseTypes = normalize(join('src', 'integrations', 'supabase', 'types.ts'));

          function readRuntimeSources(root: string): string {
            return readdirSync(root, { withFileTypes: true })
              .filter((entry) => entry.name !== '__tests__')
              .map((entry) => {
                const path = join(root, entry.name);
                if (entry.isDirectory()) return readRuntimeSources(path);
                if (normalize(path) === generatedSupabaseTypes) return '';
                return ['.ts', '.tsx'].includes(extname(entry.name)) ? read(path) : '';
              })
              .join(String.fromCharCode(10));
          }

          describe('Aegis UI and final schema compatibility', () => {
            it('uses only RPC names supported by the final Aegis runtime contract', () => {
              const runtime = readRuntimeSources('src');
              expect(runtime).toContain('get_device_copies_for_messages');
              expect(runtime).not.toContain("'aegis_sync_device'");
              expect(runtime).not.toContain("'aegis_ack_device_messages'");
              expect(runtime).not.toContain("'get_device_copy_for_message'");
              expect(runtime).not.toContain("'send_message_with_device_copies'");
              expect(runtime).not.toContain("'insert_message_with_device_copies'");
              expect(runtime).not.toContain("'call_signal'");
            });

            it('renders a pending bubble before the first server message exists', () => {
              const source = read('src/components/ChatWidget.tsx');
              expect(source).toContain(
                '(messages?.length ?? 0) === 0 && queue.pendingMessages.length === 0',
              );
              expect(source).toContain(
                'key={pm.localId} className=\"flex justify-end mt-1 px-2\"',
              );
            });

            it('does not bind mark-read to an unstable mutation result object', () => {
              const source = read('src/components/ChatWidget.tsx');
              expect(source).toContain(
                'const { mutate: markConversationRead } = useMarkConversationRead();',
              );
              expect(source).toContain('[conversationId, markConversationRead]');
              expect(source).not.toContain('[conversationId, markRead]');
            });

            it('does not force the conversations query to refetch on every widget mount', () => {
              const source = read('src/hooks/useMessages.ts');
              const start = source.indexOf('export function useConversations()');
              const end = source.indexOf('export function useMessages(');
              expect(start).toBeGreaterThanOrEqual(0);
              expect(end).toBeGreaterThan(start);

              const conversationSource = source.slice(start, end);
              expect(conversationSource).toContain("queryKey: ['conversations', user?.id ?? 'anon']");
              expect(source).toContain("queryKey: ['conversations', userId]");
              expect(conversationSource).toContain('refetchOnMount: false');
              expect(conversationSource).not.toContain("refetchOnMount: 'always'");
            });
          });
          """)
          PY

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: Focused compatibility tests
        run: npx vitest run src/lib/messaging/__tests__/aegisUiRpcCompatibility.test.ts src/lib/messaging/__tests__/aegisDeviceInbox.test.ts
      - name: Full tests
        run: npm test -- --run
      - name: Production build
        run: npm run build

      - name: Commit validated source and remove temporary audit files
        shell: bash
        run: |
          git checkout origin/main -- public/version.json
          rm -f .github/aegis-final-rpc-diagnostic.txt
          rm -f .github/workflows/aegis-final-rpc-audit.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/lib/messaging/multiDeviceFanout.ts \
                  src/lib/messaging/aegisTransport.ts \
                  src/lib/messaging/__tests__/aegisUiRpcCompatibility.test.ts \
                  public/version.json \
                  .github/aegis-final-rpc-diagnostic.txt \
                  .github/workflows/aegis-final-rpc-audit.yml
          git commit -m "fix(aegis): use verified batch device-copy RPC"
          git push origin HEAD:agent/aegis-ui-rpc-logic-fix
