    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
A    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
V    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
/    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
V    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
L    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
/    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
L    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
x    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
S    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
|    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
x    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
A    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
|    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
|    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
x    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
|    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
S    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
|    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
S    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
S    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
S    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
<    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
>    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
L    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
>    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
[    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
E    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
2    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
E    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
E    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
]    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
[    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
]    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
-    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
&    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
&    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
!    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
[    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
E    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
2    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
E    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
E    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
]    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
[    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
]    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
-    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
x    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
S    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
B    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
<    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
b    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
>    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
b    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
@    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
/    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
/    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
b    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
/    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
[    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
V    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
B    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
]    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
[    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
b    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
q    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
b    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
S    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
b    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
b    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
q    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
q    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
b    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
b    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
S    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
]    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
B    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
V    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
|    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
|    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
B    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
[    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
E    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
2    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
E    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
E    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
]    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
[    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
]    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
S    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
B    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
x    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
A    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
<    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
>    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
/    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
W    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
F    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
B    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
?    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
U    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
>    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
<    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
>    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
W    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
?    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
U    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
>    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
<    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
>    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
F    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
B    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
?    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
?    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
W    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
!    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
b    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
?    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
S    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
b    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
b    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
A    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
V    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
|    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
|    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
S    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
&    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
&    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
?    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
/    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
V    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
W    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
?    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
U    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
>    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
<    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
>    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
W    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
?    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
U    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
>    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
<    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
>    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
W    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
?    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
?    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
W    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
P    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
!    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
b    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
=    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
?    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
w    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
S    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
R    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
v    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
I    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
y    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
h    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
(    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
)    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
{    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
k    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
l    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
i    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
f    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
E    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
?    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
.    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
m    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
s    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
a    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
g    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
:    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
u    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
n    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
x    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
p    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
c    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
t    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
d    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
_    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
e    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
o    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
r    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
'    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
,    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
;    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
     // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
}    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };

    // Invariant cryptographique : la récupération passe par PIN, mot de passe
    // ou clé de récupération. Aucun chemin WebAuthn/passkey ne subsiste.
    const restored = await restoreAegisRecoveryVault(userId, attempt.key);
    if (restored.status === 'restored' || restored.status === 'already_present') {
      return finishSuccessfulRecovery(userId, 'recovery_key');
    }
    return {
      ok: false,
      source: 'recovery_key',
      reason: 'reason' in restored && restored.reason ? restored.reason : restored.status,
    };
